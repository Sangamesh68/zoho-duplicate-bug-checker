# Secrets — never commit.
# Covers .env plus variants (.env.local, .env.bak), except the template.
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Model cache (sentence-transformers downloads here)
.cache/

# Backend output from the scheduled start script
logs/

# OS
.DS_Store
